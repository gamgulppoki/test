#Spring Data Source

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/todo
    username: root
    password: swally135792468!
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    database: mysql

    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        show_sql: true
        format_sql: true
    #database-platform: org.hibernate.spatial.dialect.mysql.MySQL56InnoDBSpatialDialect